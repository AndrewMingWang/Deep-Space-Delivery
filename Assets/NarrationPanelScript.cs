using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NarrationPanelScript : MonoBehaviour
{
    // Start is called before the first frame update
    public bool canUse;

    private void Start() {
        canUse = false;
    }
}
